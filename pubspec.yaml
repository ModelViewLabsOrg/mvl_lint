name: mvl_lint
description: Custom analyzer plugin with MVL lint rules.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.1

# Add regular dependencies here.
dependencies:
  analysis_server_plugin: ^0.3.16
  analyzer: any
  analyzer_plugin: any
  collection: ^1.19.1
  path: ^1.9.1

dev_dependencies:
  analyzer_testing: any
  test: ^1.25.6
  test_reflective_loader: ^0.4.0
